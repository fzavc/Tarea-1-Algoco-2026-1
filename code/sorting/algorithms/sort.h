#ifndef SORT_H
#define SORT_H

#include <vector>

// Declaración del header para Sort.
// Se considera que la implementación está en sort.cpp.
// Contiene la declaración de la función sortArray 
// para el ordenamiento de un arreglo.

std::vector<int> sortArray(std::vector<int>& elementos);

#endif // SORT_H