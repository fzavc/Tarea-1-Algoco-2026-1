#ifndef MERGESORT_H
#define MERGESORT_H

#include <vector>

// Declaración del header para Merge Sort.
// Se considera que la implementación está en mergesort.cpp.
// Contiene la declaración de la función mergeSort 
// para el ordenamiento de un arreglo.

std::vector<int> mergeSort(std::vector<int>& elementos, int l, int r);

#endif // MERGESORT_H