#ifndef NAIVE_H
#define NAIVE_H

#include <vector>

// Declaración del header para Naive.
// Se considera que la implementación está en naive.cpp.
// Contiene la declaración de la función multiplyNaive 
// para la multiplicación de dos matrices.
std::vector<std::vector<int>> multiplyNaive(
    std::vector<std::vector<int>> &mat1,
    std::vector<std::vector<int>> &mat2
);

#endif // NAIVE_H